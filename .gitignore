*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
logs/
screenshots/
artifacts/

# Decompiled OTA APK (14k files, tens of MB). Analysis material, not source.
# Re-add a single file with: git add -f <path>
decompiled/

# Root-level adb logs
/*.log

# adb binary kept at the repo root
/adb

# Chat log imported from another assistant; 0.4MB of prose.
# git add -f it if you want it tracked.
分析内测包提取.md
